services:

  chatbot:
    build:
      target: dev
    image: shopping_assistant_chatbot:dev
    volumes:
      - ${REPO_ROOT}/chatbot:/project


  ecom-backend:
    build:
      target: dev
    image: ecom-backend:dev
    volumes:
      - ${REPO_ROOT}/ecom-backend:/project