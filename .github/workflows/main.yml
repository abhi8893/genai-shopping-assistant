name: Main workflow


on:
  push:
    branches:
      - "**"


env:
  PYTHON_VERSION: "3.12"


jobs:
  
  run_checks:
    name: Code Quality Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install "${{ env.PYTHON_VERSION }}"

      - name: Setup code quality env
        run: |
          make venv-create COMPONENT=root GROUP=ci
          make venv-switch COMPONENT=root TARGET=ci
          

      - name: Run code quality checks
        run: |
          source .venv/bin/activate
          make SKIP=no-commit-to-branch check-all

  validate_version:
    name: Validate Version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        run: uv python install "${{ env.PYTHON_VERSION }}"

      - name: Setup virtual environment
        run: |
          make venv-create COMPONENT=root GROUP=ci
          make venv-switch COMPONENT=root TARGET=ci

      - name: Validate version is greater than highest tag
        run: |
          source .venv/bin/activate
          VERSION=$(python3 scripts/ci/get_version.py --repo-root ${{ github.workspace }})
          echo "Current version: $VERSION"

          LATEST_TAG=$(git tag -l 'v[0-9]*' | sort -V | tail -n 1)
          if [ -z "$LATEST_TAG" ]; then
            echo "✓ No existing tags — version $VERSION is valid for initial release"
          else
            LATEST_VERSION="${LATEST_TAG#v}"
            echo "Latest tag: $LATEST_TAG (version: $LATEST_VERSION)"

            uv run scripts/ci/compare_versions.py "$VERSION" "$LATEST_VERSION"
          fi

  # build:
  #   name: Build packages
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
    
  #     - name: Build packages/shopping-assistant
  #       run: |
  #         make venv-create COMPONENT=shopping-assistant
  #         make venv-switch COMPONENT=shopping-assistant TARGET=prod
  #         source .venv/bin/activate
  #         cd packages/shopping-assistant
  #         uv build